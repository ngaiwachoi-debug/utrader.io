# Copy to .env.local and fill with same values as root .env
# Required for login and backend API access when running npm run dev from frontend/

NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000"

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

NEXT_PUBLIC_API_BASE="http://127.0.0.1:8000"
